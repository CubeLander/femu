# Emulator Docs Relocated

The emulator implementation documents were moved to:

1. `rv32emu/docs/` (current architecture/plans/testing docs)
2. `rv32emu/docs/legacy/` (earlier workspace-era reports)

This directory is kept as a compatibility anchor to avoid dead links in old notes.
